import { NotesHub } from "@/components/features/torah/notes/NotesHub";

// The personal notes hub — every written, scanned and recorded note in one
// place, and the save target the handwriting scanner offers by default.
export default function TorahNotesPage() {
  return <NotesHub />;
}
