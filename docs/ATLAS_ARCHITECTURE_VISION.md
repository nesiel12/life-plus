# Atlas — Architecture Vision

This is the long-term source of truth for where Atlas's intelligence layer is going. It does not replace `docs/BACKLOG.md` (near-term, actively-maintained work) or `docs/ROADMAP_V2.md` (phase sequencing) — it sits above them, describing the target shape each phase is walking toward. When a phase's work touches one of the systems below, update this document; don't fork a parallel doc for it.

**Operating principle, stated once so it doesn't need repeating in every section below:** every piece of architecture here gets built when a real feature needs it, not before. Atlas today is a single-user product with three real AI-backed endpoints. Standing up formal infrastructure for a scale or intelligence level the product hasn't reached yet is waste, not foresight — it's also actively harmful, because speculative abstractions get the shape wrong and have to be redone once real requirements show up. Each section below ends with "Current state" and "Trigger to build" for exactly this reason.

---

## 1. The shape of the whole system

```
User Data (moments, goals, people, knowledge, health, chat, calendar)
        ↓
Life Events (a moment/goal/interaction/entry actually happening)
        ↓
Memory Engine (what Atlas keeps and can retrieve about a person)
        ↓
Personal DNA Engine (who this person is — patterns, not facts)
        ↓
Context Engine (what's relevant right now, for this request)
        ↓
AI Agents (specialized reasoning over that context)
        ↓
Recommendations (surfaced to the user, always reversible, always attributable)
```

This is a data-flow model, not a service-boundary model. It does **not** imply five microservices or a message bus — at Atlas's current and near-term scale, this whole pipeline lives inside the Next.js app, mostly as plain TypeScript functions and Postgres queries. The boundaries matter for reasoning about the system, not for how it's deployed.

---

## 2. Memory Engine

**What it is:** the mechanism by which something a user does or says becomes something Atlas can draw on later — in a different conversation, a different life area, months later.

**Memory types**, mapped to what already exists:

| Type | Description | Current table(s) |
|---|---|---|
| Personal facts | Name, preferences, important dates | `users`, `people` |
| Behavioral memory | Habits, rhythm, learning style | `personal_dna` |
| Relationship memory | People, moments, interaction history | `people`, `moments` (category=`family`) |
| Knowledge memory | Books, shiurim, ideas, projects | `knowledge_entries` |
| Life memory | Milestones, achievements, reflections | `moments`, `insights`, `goals` |
| Conversational memory | Past chat exchanges, across sessions | `chat_messages` (joined AI Companion Experience v2, §10) |

**Current state — v1 (lexical retrieval) shipped and live in every AI-backed route that has a natural-language query to rank against.** `lib/memory/retrieveRelevantMemory(userId, query, limit)` ranks moments/knowledge_entries/insights/chat_messages by keyword overlap + recency and is called (via the Context Engine, §5, or directly — see Goals/Learning/Areas/Family Experience v2) from chat, goal breakdown, Torah extraction, and Goals/Learning/Areas/Family insights — each with a task-appropriate query. Calendar suggestions deliberately doesn't call it (no natural-language query to rank against). Storage existed since Phase 1; recall now spans four entity types, not three — `chat_messages` joined the pool for AI Companion Experience v2's "reference previous conversations" requirement, real value once a conversation grows past the 50-message window `/api/chat` sends per request.

**Fetch-once, rank-many (Atlas Core Optimization v1).** The single-query call sites above (chat, goal breakdown, Torah extraction) each call `retrieveRelevantMemory` once per request — fine as-is. The four `*/insights` routes don't: each ranks against a *different* query per item in a loop (once per goal, per knowledge entry, per life area, per person), and originally each of those calls independently re-fetched all four source tables — real, measurable N+1 query behavior, invisible at today's near-empty data volume but not free. `lib/memory/retrieveMemory.ts` now splits the function in two: `fetchMemoryCandidates(userId)` does the I/O once, `rankMemoryCandidates(candidates, query, limit)` is a pure, synchronous rank over an already-fetched pool. `retrieveRelevantMemory` itself is an unchanged-signature, unchanged-behavior composition of both, for the callers that never needed the split. Net effect: a Family page with N people now does 4 memory queries per request instead of 4×N, with zero change to any ranked result.

**Target:** the function signature above already is the target for v1 — what's left is v2/v3 below, gated on real evidence they're needed, not built ahead of it.

**Path, in order:**
1. **v1 — lexical retrieval.** Rank existing rows (moments, knowledge_entries, insights) by simple keyword/recency scoring, entirely in application code, no schema change. Same technique already used by Torah Space's `findRelatedSessions`. Good enough at single-user, hundreds-of-rows scale, and ships a real "Atlas remembers" experience immediately.
2. **v2 — Postgres full-text search** (`tsvector` columns + GIN indexes) once keyword relevance in v1 visibly misses things a straightforward text index would catch. Still no new infrastructure — Postgres native.
3. **v3 — semantic (embeddings) retrieval** via `pgvector` (Supabase supports it natively) once (a) data volume makes lexical/full-text recall noticeably weak, or (b) a feature genuinely needs conceptual similarity rather than keyword overlap (e.g. "what have I written that relates to this, even if it doesn't share words"). This is a real schema addition — an `embedding vector(1536)` column and an embedding-generation step on write — and should not happen before v1/v2 prove insufficient.

**Trigger to build v1:** now — see §8, this is the first concrete feature this document leads into.
**Trigger to build v2/v3:** v1 shipped and observed to miss relevant results in real use; or per-user row counts grow past what a single unindexed scan handles comfortably (low thousands).

---

## 3. Personal DNA Engine

**What it is:** the durable behavioral/preference profile — not facts about the person, but *patterns* in how they operate. This now has two distinct layers, and the naming is deliberately kept separate because they're epistemically different:

| Layer | What it holds | Where | Origin |
|---|---|---|---|
| **Stated** (`personalDNA`) | `peak_focus_hours`, `learning_style`, `family_check_in_interval_days`, `habit_notes` | `personal_dna` table, one row/user | The user said so, at onboarding |
| **Inferred** (`personalPatterns` — the Personal DNA *Engine*) | Behavioral patterns with a confidence score | `personal_patterns` table, many rows/user | Atlas noticed it, from his actual data |

The stated layer shipped in Phase 1 and got its first real consumers earlier this session (chat system prompt, family page's stale-contact threshold). This section is about the inferred layer — **v1 is shipped.**

**Current state — v1 shipped.** `lib/intelligence/personalDNA/` is a self-contained module: every analyzer is a pure function (raw rows in, `PatternCandidate[]` out — no DB access, fully unit-tested), and exactly one server-only file (`analyze.ts`) does the fetching and writing. Four categories, all backed by real, already-collected data — nothing invented to fill a category that had no signal:

- **Focus** (`analyzers/focus.ts`) — per life-area category, the time-of-day window (`moments.occurred_at`, converted to Asia/Jerusalem local time — see below) where the user logs the most moments. *"הרגעים בתחום ידע מתועדים בעיקר בין 18:00–22:00."*
- **Learning** (`analyzers/learning.ts`) — recurring topic words across `knowledge_entries` (reuses `lib/memory/rankRelevance.ts`'s `tokenize`), and study-session cadence (entries/week over the observed span).
- **Goals** (`analyzers/goals.ts`) — task-size preference (does he finish more when goals are broken into many small milestones or few large ones — a real correlation across his own goal history, not a guess), goal momentum/stagnation (share of open goals untouched 14+ days), and milestone completion pace (needs `milestones.completed_at`, added in migration `20260720000003` — **only populates going forward**; milestones marked done before that migration have no pace evidence, deliberately left `null` rather than backfilled with a guessed timestamp).
- **Routine** (`analyzers/routine.ts`) — most-active weekday, and a 30-day activity-consistency score, both across the union of moments + knowledge entries.

**Health is explicitly not a category yet** — `health_logs` exists as a table (migration `20260720000001`) but has no write path anywhere in the app (the Health area page is just `AreaMomentsView`, generic moments UI). Building a health pattern today would mean analyzing zero rows and either producing nothing (fine, but pointless code) or being tempted to fabricate signal from `moments` tagged `health` (a much weaker proxy). Left out until the Health area gets a real logging flow.

**Confidence model** (`confidence.ts`) — deterministic, no ML:
- `calculatePatternConfidence(evidenceCount, strength)`: `strength` (0–1, how concentrated/clear the signal is) times an evidence-saturation curve (`evidenceCount / (evidenceCount + 5)`, so ~5 data points ≈ half-trust) times `MAX_CONFIDENCE` (0.95 — Atlas never claims certainty about a person). A brand-new, single-evidence assumption lands around 0.15–0.2; it climbs as more evidence accumulates, asymptotically, never touching 1.0.
- `resolvePatternUpdate(existing, fresh)`: the actual "DNA update" step. Recomputing a pattern that agrees with what's already stored uses the fresh (larger) evidence count as-is — confidence climbs naturally because there's more data behind the same belief. Recomputing a pattern whose *value* flipped (a genuine contradiction) applies a 0.7× discount to the fresh confidence — the new belief isn't instantly trusted, it has to earn that back over subsequent runs.
- `rankPatterns`: filters to `MIN_CONFIDENCE_TO_SURFACE` (0.3) and sorts by confidence — the one place "which beliefs are trustworthy enough to act on" is decided, so nothing downstream re-implements its own threshold.

**Data flow (the self-learning loop, concretely):**
```
User acts (logs a moment, completes a milestone, studies a shiur)
        ↓
app/actions/bootstrap.ts's getInitialState() runs (every app open)
        ↓  (after() — doesn't block the response)
analyzePersonalDNA(userId): fetch raw data -> run 4 analyzers -> get PatternCandidate[]
        ↓
For each candidate: resolvePatternUpdate(existing stored pattern, fresh candidate)
        ↓
personalPatternsRepo.upsert (one row per category+patternType+subject)
        ↓
Read path: getPersonalPatternDescriptions(userId) -> rankPatterns -> top 5 confident descriptions
        ↓
lib/context/buildAtlasContext.ts includes them as AtlasContext.personalPatterns
        ↓
Chat / goal breakdown / calendar suggestions fold them into their prompts/rationale
        ↓
(Not yet closed): user accepts/rejects/acts differently -> feeds back into future evidence
```
The trigger is deliberately simple — re-analyze once per app open, not per-mutation and not on a schedule (both would be over-engineering ahead of evidence they're needed; see §4's Notification Agent note for where a real scheduler eventually belongs). `next/server`'s `after()` runs it post-response so the user never waits for it.

**Integration, this session:**
- **Context Engine** — `AtlasContext.personalPatterns: string[]`, fetched alongside everything else in `buildAtlasContext`.
- **Chat** — a new system-prompt section, explicitly labeled as inferred-not-stated so the model doesn't present it as something the user said.
- **Goal breakdown** — patterns fold into the same context block active goals and memory already use, so a `taskSizePreference` belief can actually shape milestone count/size.
- **Calendar suggestions** — foundation only, as scoped: ranking (which area/slot wins) is untouched; a matching focus-window pattern for the suggested area now enriches the *rationale* text. No energy/focus-window-based re-ranking yet — that's the next increment, not this one.

**What's still open, honestly:**
- The feedback half of the loop (accept/reject/modify → confidence adjustment) isn't wired — `docs/ATLAS_ARCHITECTURE_VISION.md` §7 (self-improvement loop / `recommendation_events`) is the natural place this lands, once that table exists.
- `analyzePersonalDNA` and `lib/db/personalPatterns.ts` have no automated test (DB-dependent, same gap as `buildAtlasContext` — see §5). Every *analyzer* it calls is fully unit-tested; the orchestration wiring itself isn't yet.
- Calendar suggestions doesn't rank by focus window — deliberately deferred per this milestone's scope.

**Trigger for v2:** real usage accumulates (weeks, not days) such that patterns currently sitting below `MIN_CONFIDENCE_TO_SURFACE` start crossing it, or a concrete feature need justifies calendar re-ranking by energy/focus windows.

---

## 4. AI Agent System

**What it is, per the product vision:** specialized reasoning units (Memory, Calendar, Learning, Torah, Relationship, Health, Goal, Reflection, Analytics, Planning, Notification) that all communicate through an "Atlas Core."

**Current state — read this carefully before building anything here:** Atlas already has the *functional* equivalent of several of these "agents," and they work, because they were built as focused server-side functions with clear inputs/outputs rather than as a formal multi-agent framework:

| Named agent | Current implementation |
|---|---|
| Calendar Agent | `app/api/calendar/suggestions` (ranks free slots by weakest life area) + `app/api/calendar/events` (writes accepted suggestions to Google Calendar) |
| Goal Agent | `app/api/goals/breakdown` (LLM-generated milestone breakdown) + `app/api/goals/insights` (per-goal stage/progress/next-action, Goals Experience v2 — §10) |
| Learning/Torah Agent | `app/api/torah/extract` (PDF/audio → topic/source/summary) + `app/api/torah/insights` (streak/progress/next-review, Learning Experience v2 — §10) + `app/api/torah/study-material` (on-demand flashcards/review questions, same AI provider, no new capability) |
| Reflection Agent | `AICompanion` chat — streamed replies, a real per-reply "based on" rationale, and a proactive `/api/briefing`-powered opener (AI Companion Experience v2 — §10) |
| Analytics Agent (partial) | `app/api/areas/insights` (per-area health/attention/momentum, Areas Experience v2 — §10) |
| Relationship Agent | `app/api/family/insights` (per-person health/priority/suggested next interaction, Family Experience v2 — §10) |

**What's genuinely missing** isn't more agents — it's a Memory Agent (§2) and any Analytics/Notification/Planning agent at all, because there's no scheduled/background execution model yet (everything today is user-triggered, request/response).

**Target — and the important architectural call:** do **not** build a formal agent framework (orchestrator, message bus, agent registry) speculatively. Every "agent" above is a well-scoped server function, independently testable, independently deployable, and that has been the right size for the actual problem so far. The point at which a real framework earns its cost is when either (a) agents need to call *each other* dynamically at runtime (not just share data through Postgres), or (b) there's a genuine scheduling/orchestration need (see Notification Agent below) that a cron job can't express cleanly. Until then, "Atlas Core" is Postgres plus shared TypeScript modules (`lib/`) — every agent already reads/writes the same tables and the same mapper functions, which *is* the integration layer, just not a ceremonial one.

**The one new capability this implies:** a Notification/Planning "agent" needs something none of the current agents have — the ability to run *unprompted*, on a schedule, not in response to a request. That's a real gap (see `docs/BACKLOG.md`'s Future Vision: "a genuine unprompted background AI behavior"). When that gets built, it should be a Vercel Cron / Supabase Cron job calling the same server-side functions the request-driven routes already call — not a new execution model.

**Trigger to build a real orchestration layer:** a concrete feature requires one agent's output to dynamically decide which other agent runs next, at runtime, with branching — not before.

---

## 5. Context Engine

**What it is:** the layer that decides, for a given moment (a chat message, a page load, a scheduled job), what subset of memory + personalDNA + recent activity is actually relevant to assemble into a prompt or a UI.

**Current state — shipped (Context Engine v1).** `lib/context/buildAtlasContext(userId, { query? })` is the one place that assembles personalDNA, active goals, life-area scores, upcoming events, relationship signals (stale-contact detection, reusing the same rule as the family page), and — when a query is given — Memory Engine (§2) retrieval, all in one parallelized fetch. It returns `AtlasContext`, a plain object of pre-formatted strings/rows that every AI-backed route now consumes instead of independently fetching and formatting its own slice:

| Route | Query passed | What changed |
|---|---|---|
| `/api/chat` | the user's message | Previously fetched personalDNA + memory inline; now one `buildAtlasContext` call feeds the whole system prompt (personalDNA, life-area balance, active goals, upcoming events, relationship signals, relevant memory). |
| `/api/goals/breakdown` | the new goal's title | Previously had zero user context at all — a bare title+category prompt. Now sees the user's other active goals (avoid redundant milestones) and related past memory, and adapts to `learning_style` when set. |
| `/api/torah/extract` | the extracted/transcribed text | Previously had zero user context. The summarization call now sees related past knowledge/moments, so a new shiur can be summarized with awareness of what he's studied before. |
| `/api/calendar/suggestions` | — (no query; not memory-driven) | Previously trusted whatever life-area scores the *client* sent in the request body. Now fetches its own canonical scores server-side, and a family-category suggestion's rationale names a specific overdue contact when `relationshipSignals` has one. |

`lib/context/formatContext.ts` (`formatContextSection`/`joinContextSections`) is the one shared "turn a list of facts into a titled bullet block" helper — previously duplicated ad hoc inside `lib/chatSystemPrompt.ts`; now every route above uses it instead of hand-rolling prompt-section formatting.

**What's still route-specific, deliberately:** each route decides *how* to fold `AtlasContext` into its own task-specific prompt — chat's system prompt looks nothing like the goal-breakdown system prompt, and shouldn't. The Context Engine unifies *gathering*, not the reasoning task built on top of it — that's the line that keeps this from becoming "one AI implementation forced to serve four features."

**Target, next increment:** `buildAtlasContext` itself has no automated test yet (it's a thin composition of repo calls + the same formatting logic already tested elsewhere, but "thin" isn't a substitute for tested — see `docs/BACKLOG.md`). Beyond that, this module is the natural place a future Analytics/Notification agent (§4) would also read from, once one exists.

**Trigger to build v1:** done, this session — see the commit history around this section.
**Trigger for the next increment:** an integration-test setup that can exercise `buildAtlasContext` against a real or mocked Supabase client (currently blocked on the same "no integration test harness yet" gap as every other DB-backed route).

---

## 6. Life Timeline — Timeline Experience v1

**What it is:** per the product vision, a visual, chronological view across every life area — books, studies, projects, achievements, relationships, goals — as "a personal history of growth," not a database table.

**Current state — v1 shipped.** `app/timeline/page.tsx` is a true cross-entity feed, not a moments-only log. `lib/timeline/buildTimelineEvents.ts` (pure, no DB access, fully unit-tested — the same "raw rows in, typed events out" shape Personal DNA's analyzers already established, §3) merges three already-hydrated store slices into one sorted `TimelineEvent[]`:
- every `moment`, unconditionally, across all categories (unchanged behavior);
- a `goal_started` event per goal — real narrative value ("you set out to do X on this date"), not just a progress bar;
- a `milestone_achieved` event per *completed* milestone, placed at its real `completed_at` timestamp, never fabricated;
- a `knowledge_session` event per Torah Space entry (`knowledgeEntries`), mapped to the `faith` life area — the only feature that writes that table today.

`lib/timeline/groupEvents.ts` groups the sorted output into calendar-day buckets ("היום"/"אתמול"/weekday name/full date), reusing `lib/utils.ts`'s `daysSince` instead of a second date-diffing implementation — the same relative-day vocabulary the Family page and upcoming-events list already use.

**A real, small backend gap this milestone closed:** `Milestone.completedAt` didn't exist at the app-type boundary. `completed_at` was written to Postgres (migration `20260720000003`) and read raw by Personal DNA's `analyzers/goals.ts`, but `lib/mappers.ts`'s `toGoal` dropped it before it ever reached the store — nothing before this needed it client-side. Threaded through (`types/index.ts`, `lib/mappers.ts`, `store/useAtlasStore.ts`'s optimistic `toggleMilestone` update) because "highlight achievements" needed a real completion moment, not a guess. Milestones completed before that migration still have no `completedAt` — left off the Timeline entirely rather than backfilled with an invented date, the same rule Personal DNA already follows for the same column.

**UI.** Category filter chips (`LIFE_AREA_LIST` + "הכל") reuse the exact chip pattern `QuickCapture` already established — no new filter chrome. Achievement events get a distinct icon (`Award`) and a small "הישג" badge; goal-start events use `Target`; everything else uses the same per-life-area icon/color `lib/lifeAreas.ts` already defines for every other screen — one iconography system, not two. Motion, spacing, `glass-card`, and focus conventions are unchanged from the rest of the app.

**Extensibility, without speculative infrastructure.** `buildTimelineEvents` documents its own extension point inline: a future life area that's just another `moments` category (Health, once it gets a real write path) needs zero Timeline changes — moments already flow through unconditionally, for every category, today. A future feature with its own entity (the way `knowledgeEntries` is to Torah) needs one small mapper function merged into the same array — no registry or plugin system was built ahead of a second real case needing one.

**Deliberately not built this milestone:** Health events (no write path exists anywhere in the app yet — same reasoning Personal DNA's health-category exclusion already documents, §3); pagination (the existing `docs/BACKLOG.md` gap — merging in goals/milestones/knowledge makes it somewhat more relevant, not solved); reusing the Context/Memory/Intelligence Engines' *ranking* logic for "what matters" here — those operate on already-formatted prose strings assembled server-side for an LLM prompt (§5/§9), the wrong shape for a client-rendered list of raw dated events, so importing them would have been forcing a fit rather than genuine reuse. What *was* reused: `lib/lifeAreas.ts` (category presentation) and `lib/utils.ts`'s `daysSince` (date-diffing) — both unchanged, both single sources of truth.

**Trigger for the next increment:** a real Health write path ships (the Timeline picks it up for free, no code change needed); or real usage shows the flat chronological view needs its own "what matters most" ranking — at which point it's worth asking whether that's a Timeline-specific rule or a genuine fifth consumer of the Intelligence Engine (§9's own trigger already names a fifth AI-backed surface as the signal that abstraction is worth generalizing further).

---

## 7. Intelligence Layer / Self-Improvement Loop — Recommendation Intelligence & Feedback Loop v1

**What it is:** tracking which AI recommendations get accepted, rejected, or modified, and feeding that back into future suggestions. This is the missing half of the loop the rest of this document describes: §3 (Personal DNA) infers patterns from what the user *does*; this layer records what Atlas *suggested* and what the user did about it — the ingredient §3's own "What's still open" section flagged as not yet wired.

**Current state — v1 shipped.** `recommendation_events` (migration `20260720000005`) is a durable log, one row per suggestion Atlas ever surfaces: `type`, `source`, `recommendation_payload` (jsonb — the suggestion's actual content, so later analysis never needs to reconstruct it from a join), `status`, `metadata` (jsonb, deliberately unused today beyond `{}` — headroom for whatever a future consumer needs without a schema change), `created_at`, `responded_at`.

**Status model** — five states, but only one has any valid outgoing transition:
```
pending → accepted | rejected | modified | expired
```
Once a user has responded (or a suggestion times out unanswered), that's final for v1 — there's no "un-reject" flow. Enforced twice, deliberately redundantly: atomically in `lib/db/recommendationEvents.ts`'s `recordOutcome` (a conditional `UPDATE ... WHERE status = 'pending'`, not a read-then-write — race-safe by construction), and explainably in `lib/intelligence/recommendations/feedback.ts`'s `isValidStatusTransition` (pure, unit-tested, the human-readable statement of the same rule).

**Feedback weighting** (`feedback.ts`, deterministic, -1..1):
| Status | Weight | Why |
|---|---|---|
| accepted | 1.0 | Reinforces future suggestions like this one |
| modified | 0.3 | Soft positive — the user engaged and adapted it rather than rejecting it outright; the shape was roughly right |
| rejected | -1.0 | Discourages future suggestions like this one |
| expired | -0.2 | Atlas's operational meaning of "ignored" — per explicit instruction, silence is never treated as a strong rejection |
| pending | 0.0 | No signal yet |

`calculateFeedbackConfidenceAdjustment` averages weight across a group of events into one signal; `summarizeRecommendationOutcomes` groups by `type` and only reports a group once it has ≥3 responses (noise floor, same reasoning as Personal DNA's evidence thresholds). Both are pure and fully unit-tested — no ML, every number traceable to actual accept/reject counts.

**Integration, this session — tracking added to the two surfaces that generate real suggestions today:**
- **Calendar suggestions** — each suggestion becomes a `pending` `recommendation_event` *before* it reaches the client; its real database id (not a client-random one, as before) becomes `SuggestedAction.id`. `acceptSuggestion`/`dismissSuggestion` in `store/useAtlasStore.ts` now also call `recordRecommendationOutcomeAction` (a thin Server Action) after the real action completes — accept records `accepted`, the explicit "ignore" button records `rejected` (it's an active user choice, not passive silence, so the stronger weight is correct).
- **Goal breakdown** — recorded as `accepted` at generation time, not `pending`: `GoalsPanel` has no review step (the returned milestones are applied to a new goal the instant they arrive), so a `pending` event here would be one nothing could ever transition out of. Payload includes `usedAI: boolean` so the real-AI and generic-fallback paths (`/api/goals/breakdown`'s existing honest-fallback behavior) are distinguishable in later analysis.
- **Chat and Torah — deliberately not touched.** Chat doesn't currently generate a structured, actionable suggestion a user can accept/reject (just conversation) — there's nothing to track yet. Torah extraction's interface (`createRecommendationEvent`'s `type`/`source` are free strings, no schema change needed) is ready for it, per this milestone's explicit "prepare the interface, don't build speculative flows" instruction — no Torah-specific code was added.

**Read path — `getRecommendationInsights(userId)`:** feeds `AtlasContext.recommendationInsights` (Context Engine, §5) with the highest-signal summaries (`"הצעות ליומן: מתקבלות בכ-80% מהמקרים (4 מתוך 5)."`), surfaced in chat's system prompt alongside personalPatterns. Starts empty — like every other inference layer in this document, it organically populates as real accept/reject events accumulate, rather than being seeded with anything invented.

**Deliberately not built this session** (per explicit scope and "clean interfaces, no tight coupling"):
- **Personal DNA does not consume recommendation feedback yet.** `resolvePatternUpdate` (§3) and `calculateFeedbackConfidenceAdjustment` (this section) are two separate, independently-testable confidence mechanisms — the former reconciles a pattern against its own history, the latter aggregates suggestion outcomes. Wiring feedback *into* pattern confidence (e.g., a rejected `family`-category calendar suggestion nudging that life area's `peakActivityWindow` pattern down) is real future value, but doing it now would mean designing a coupling contract ahead of evidence for what it should actually look like.
- **No active `expired` sweep.** The status exists and is fully handled by the feedback model, but nothing yet marks a long-pending suggestion `expired` — that needs a scheduled job, which is Notification Agent (§4) territory and equally deferred there.
- **Calendar suggestion ranking still doesn't consult `recommendationInsights`** — same "foundation, not rebuild" scope as §3's calendar integration.

**Trigger for the next increment:** enough `accepted`/`rejected` volume exists that a concrete surface (most likely calendar suggestions, since it already has the richest signal) would visibly benefit from feedback-adjusted ranking — at that point, design the Personal DNA coupling deliberately rather than reactively.

---

## 8. Database evolution

Current schema (`supabase/migrations/`) — 15 tables, all user-scoped (directly or via a foreign key): `users`, `life_area_scores`, `people`, `moments` (now with `person_id` — migration `20260720000007`, Family Experience v2, a real structural link to `people` replacing the previous name-in-prose convention), `upcoming_events`, `knowledge_entries` (with `last_reviewed_at`/`flashcards`/`review_questions` — migration `20260720000006`, Learning Experience v2), `daily_intentions`, `personal_dna`, `goals`, `milestones` (with `completed_at`), `chat_messages`, `insights`, `health_logs`, `personal_patterns`, `recommendation_events`. Every migration through `20260720000005` is applied and verified live; `20260720000006` and `20260720000007` are written and typed against (`types/database.ts`) but haven't been run against the real database from this environment yet — see `docs/BACKLOG.md`.

This schema already *is* the Memory Engine's storage layer (§2), the Personal DNA Engine's storage layer (§3), and — as of this session — the Recommendation Intelligence layer's storage (§7). The near-term database work implied by this document, in priority order:

1. **Nothing, for Memory Engine v1** — it reads existing tables. *(done)*
2. **`recommendation_events`** (§7) — small, additive, no dependencies. *(done, migration `20260720000005`)*
3. **`tsvector` + GIN index columns** on `moments`, `knowledge_entries`, `insights` — for Memory Engine v2, once v1 shows the need.
4. **`pgvector` extension + `embedding` columns** — for Memory Engine v3, gated as described in §2.

RLS stays exactly as documented in `docs/BACKLOG.md`: enabled fail-closed on every table, with `user_id` filtering in `lib/db/*.ts` as the actual enforcement boundary while the service-role key is how the app talks to Postgres. This document doesn't change that — it's a deliberate, documented, revisit-when-a-feature-queries-Supabase-from-the-client decision, not an oversight.

---

## 9. Atlas Intelligence Engine — the unified decision pipeline

**What it is:** the layer that turns four independent intelligence sources (Memory §2, Context Engine §5, Personal DNA §3, Recommendation feedback §7) into one deterministic, explainable, ranked signal every AI-backed route consumes — instead of each route independently deciding, by hand, which facts to mention and in what order.

**This is explicitly not an agent, an orchestrator, or a workflow engine.** There's no event bus, no autonomous decision-making, no ML, no embeddings beyond what Memory Engine v1 already uses (plain keyword ranking), no prediction. It's a pure, synchronous transformation of data the Context Engine already fetched — `AtlasContext in, ranked signals out` — that happens to live in its own module because the transformation itself (normalize → rank → check for competing priorities → render) is now genuinely shared across four call sites, not because it needed a grander name.

**Why now, not before — the audit that justified it:** before this milestone, `lib/chatSystemPrompt.ts`, `/api/goals/breakdown`, and `/api/torah/extract` each independently called `joinContextSections([formatContextSection(...), ...])` with an author-picked, hardcoded section order and category subset — the same "assemble text from AtlasContext" pattern written three times, differing only in which sections and what order. `/api/calendar/suggestions` had a lighter version of the same problem: `relationshipSignals[0]` and `personalPatterns.find(...)` — "pick whichever matters most," decided by array position rather than any actual comparison. That's real, demonstrated duplication (not a hypothetical one), which is what justified building this rather than leaving each route to hand-roll its own ordering indefinitely.

**Signal lifecycle — the five stages:**

```
AtlasContext (already fetched by the Context Engine, §5)
        ↓  Stage 1: Collect — nothing to do here; §5 already did it
AtlasContext's 8 fields (personalDNA, activeGoals, lifeAreas, upcomingEvents,
relevantMemory, relationshipSignals, personalPatterns, recommendationInsights)
        ↓  Stage 2: Normalize — buildIntelligenceSignals()
IntelligenceSignal[] — every item classified into one of 8 categories, with
category-level importance/confidence and (currently flat) recency
        ↓  Stage 3: Rank — rankSignals()
RankedSignal[] — score = 0.5·importance + 0.3·confidence + 0.2·recency,
sorted descending, deterministic category-priority tie-break
        ↓  Stage 4: Conflict detection — detectPriorityConflicts()
PriorityConflict[] — competing high-ranked categories flagged, not resolved
        ↓  Stage 5: Prompt assembly — formatSignalsForPrompt()
Plain text, ready to drop into a system prompt or rationale string
```

**Stage 2 — normalize** (`lib/intelligence/core/normalize.ts`). `buildIntelligenceSignals(context: AtlasContext)` maps every field to a `SignalCategory` (`personalDNA`, `personalPattern`, `goal`, `lifeArea`, `upcomingEvent`, `relationship`, `memory`, `recommendation`). A documented, deliberate simplification: AtlasContext's fields arrive as **already-formatted strings** (§5's own design — "callers don't need to know the underlying entity shapes"), which means the original per-item numeric confidence some of these came from (a personalPattern's real confidence score, a memory item's real recency) was already discarded before reaching this boundary. Recovering it would mean widening AtlasContext's contract for every existing consumer just to serve this one new one — not justified by real duplication, so it wasn't done. Instead, normalization applies **category-level** defaults (a stated preference is more certain than an inferred pattern; a dated commitment is more certain than a possibly-stale memory) — every default is a named constant in `CATEGORY_DEFAULTS`, not a magic number. One real per-item signal *is* computed here: a life area scoring below 40 gets an importance boost, the same "weakest area matters most" rule calendar suggestions already ranked by, now expressed once instead of reimplemented.

**Stage 3 — rank** (`lib/intelligence/core/rank.ts`). `score = 0.5·importance + 0.3·confidence + 0.2·recency` — importance weighted highest (it's the category-level "does this matter" judgment), confidence next (so a low-certainty inference can't outrank a well-evidenced one just by being tagged important), recency smallest. **Honest limitation, stated plainly:** recency is flat at 1.0 for every signal in v1, because real per-item recency isn't recoverable at the AtlasContext boundary either (same reasoning as Stage 2) — the term stays in the formula, not hardcoded away, so a future signal source that does carry real timestamps slots in without changing the scoring contract. Ties break on an explicit, documented category-priority order (dated commitments → active commitments → relationships → what Atlas knows about the person → retrieved history → ambient state → aggregate feedback), then on input order via `Array.prototype.sort`'s guaranteed stability (ES2019+) — fully deterministic, verified by a dedicated stable-ordering test.

**Stage 4 — conflict detection** (`lib/intelligence/core/conflicts.ts`), scoped honestly. True temporal conflict detection ("this suggestion collides with a real calendar slot," the kind of reasoning in this milestone's own illustrative example) would need structured start/end times on every signal type — most (goals, relationships, memory, personalDNA) don't carry one, for the same reason recency doesn't. What v1 actually detects: **priority competition** — among the top 5 ranked signals, category pairs that routinely compete for the same attention (`goal`↔`relationship`, `goal`↔`upcomingEvent`, `relationship`↔`upcomingEvent`) get flagged with a plain-language note. This is real and useful — it tells a caller "you're choosing between two genuine priorities" — without pretending to a scheduling-collision capability the data doesn't support. Checked against the *top of the ranking* rather than an absolute score threshold: an absolute cutoff turned out to be miscalibrated against `CATEGORY_DEFAULTS` during development (most categories clear a naive 0.7 threshold by construction, regardless of content) — using rank position instead sidesteps re-tuning a threshold against defaults that live in a different file.

**Stage 5 — explainability & rendering** (`lib/intelligence/core/format.ts`). Every `RankedSignal` carries a `reason` string (e.g., "חשיבות גבוהה, ביטחון גבוה") — Atlas can always answer "why is this here" for any signal, even though `formatSignalsForPrompt` doesn't inline the full reason into every prompt line (would bloat the prompt for no benefit the LLM needs). What it *does* inline: a `(ביטחון נמוך)` hedge on any signal below 0.5 confidence, so a weak inference is never rendered with the same authority as a certain fact — literally "if confidence is low, say so," as instructed. **AI Companion Experience v2 (§10) is the first surface to expose this explainability to a person, not just the model:** `lib/chatSystemPrompt.ts`'s `buildSystemPrompt` now returns the `RankedSignal[]` it used alongside the prompt string, and `/api/chat` surfaces the top few as a real "based on" list next to the reply — the same signals, not a second explanation invented after the fact.

**Integration — four surfaces, functionally-identical behavior, infrastructure-only change:**
| Route | Categories considered | What changed |
|---|---|---|
| `/api/chat` (`lib/chatSystemPrompt.ts`) | all 8 | Previously 8 hardcoded titled sections in a fixed order; now one ranked list, competing-priority notes surfaced when relevant. |
| `/api/goals/breakdown` | personalDNA, personalPattern, goal, memory | Previously a hand-picked 3-section block plus a separately-special-cased `learning_style` note; now personalDNA is a normal signal like everything else, one call replaces both. |
| `/api/torah/extract` | memory | Scope unchanged (it only ever used `relevantMemory`) — same information, now ranked instead of dumped in arrival order. |
| `/api/calendar/suggestions` | relationship, personalPattern | Not an LLM prompt — enriches rationale text. `relationshipSignals[0]` → top-ranked relationship signal; `personalPatterns.find(...)` → top-ranked matching personalPattern signal. Suggestion *ranking* (which life area, which slot) is untouched. |

**Performance:** the engine adds zero DB queries — it's a pure, synchronous transform over data `buildAtlasContext` already fetched in one parallelized `Promise.all` (§5, unchanged). Normalizing and ranking a few dozen signals is a single `O(n log n)` sort; each route calls it exactly once per request. No new allocation pattern beyond mapping small arrays. The engine is not on a hot path different from where `buildAtlasContext` already was.

**What's deliberately not built:** literal calendar-collision conflict detection (§ above); real per-signal recency (needs AtlasContext's contract to widen, not justified by current duplication); feeding `detectPriorityConflicts`' output into anything beyond a chat-prompt note (e.g., actually re-ranking calendar suggestions around a detected conflict); any structured coupling to Personal DNA confidence (`resolvePatternUpdate`, §3) or recommendation feedback (`calculateFeedbackConfidenceAdjustment`, §7) beyond both already being inputs to the *same* signals this engine ranks — genuinely a v2 concern, once real usage shows what the coupling should look like.

**Trigger for the next increment:** a fifth AI-backed surface ships (validates the abstraction generalizes past four hand-picked call sites), or real per-item recency becomes available at the AtlasContext boundary for a concrete reason (e.g., Memory Engine v2's full-text search naturally exposing it) — at that point recency stops being a constant and the scoring formula's weights are worth re-examining against real data instead of the initial, reasoned-but-untested 0.5/0.3/0.2 split.

---

## 10. Experience Layer — making the intelligence visible

**What it is:** the product-experience discipline that governs how everything §1–§9 built actually reaches the user. Every prior section was backend: memory, context, inferred patterns, feedback, ranking. None of it was visible anywhere except folded into LLM prompts. This section is about exposure, not new capability — no new engine, no new AI architecture, explicitly out of scope per this milestone's own constraints.

**Interaction philosophy.** Atlas should never read as a dashboard, a spreadsheet, a form, an admin panel, or a database viewer — every screen answers *"what matters right now"*, not *"here is all your data."* Concretely, that means: a screen leads with synthesis (a ranked, prioritized view) before it lists raw records; empty states are quiet, not gamified ("no data yet, keep going!" is a dopamine loop, not calm design — an empty `AIBriefing` simply doesn't render, the same way `ScheduleSuggestions` already did nothing rather than show a fake "you're all caught up!" card); and nothing is invented to fill a section that has no real signal behind it yet.

**UI principles, applied this milestone:**
- **Reuse before inventing.** `AIBriefing`'s confidence-adjacent styling reuses the exact thin-progress-bar primitive `GoalsPanel` and the areas hub already use for score/progress — a new visual language for "here's a number 0–100" would have been an inconsistency, not an improvement. Every new component (`AIBriefing`, the redesigned `ScheduleSuggestions` cards) is built from `GlassCard`, the existing accent-color tokens, and the existing spacing scale — no new design tokens introduced.
- **No fabricated personalization.** The dynamic greeting (`lib/greeting.ts`) is real — computed from the browser's actual clock, not a canned rotation of phrases. Calendar suggestions' new confidence indicator (`lib/suggestionConfidence.ts`) is a real, deterministic function of how far a life area lags the average of all areas — not an invented "AI confidence" number with no basis. Where a genuinely real signal wasn't cheaply available (a "you completed two milestones yesterday" achievement line would have needed exposing `milestones.completed_at` further up the stack than this milestone touches), the section was left out rather than faked — see `docs/BACKLOG.md`.
- **Explainability made concrete, not just theoretical.** §9 already gave every ranked signal a `reason` and a confidence-based hedge; `AIBriefing` is the first surface where a person, not a model, reads that hedge (`ביטחון נמוך`) directly.

**Motion philosophy.** Nothing new was introduced — the existing conventions (`framer-motion`, 0.25–0.5s durations, `easeOut`, `y`-offset fades, no spring/bounce physics, staggered `delay` per list item) were already correct and are what every new component (`AIBriefing`'s skeleton-to-content transition, the redesigned suggestion cards) follows. Motion communicates arrival order and hierarchy (later `delay` = lower priority on the page), never decoration.

**Decision transparency.** Every number shown to the user in this milestone traces to a named, documented, testable function: `computeSuggestionConfidence` (suggestion confidence), `rankSignals`'s `score`/`reason` (briefing ordering and its `ביטחון נמוך` hedge), `detectPriorityConflicts` (the gentle "competing priorities" note). Nothing in the Experience Layer is a black box the backend can't already explain — this section exposes §9's explainability model, it doesn't add a second one.

**What shipped this milestone (Experience Layer v1) — audited, prioritized, and scoped from the full brief, not attempted wholesale:**
- Dynamic, real time-of-day greeting (`app/page.tsx`, `lib/greeting.ts`).
- `AIBriefing` — the flagship "what matters right now" card, `app/api/briefing/route.ts` + `components/features/AIBriefing.tsx`, the first non-LLM consumer of the Intelligence Engine (§9).
- Upgraded recommendation cards (`ScheduleSuggestions.tsx`): real effort (slot duration), real confidence (a progress bar, reusing the existing primitive), clearer accept/dismiss actions. "Modify" was deliberately not added as a UI action — no real modify flow exists anywhere in the app yet (`recommendation_events.status` supports `modified`, but nothing produces it), and a button with no real behavior behind it would be exactly the fabricated interaction this layer exists to avoid.
- Today page reassembled around the briefing as the hero, without touching any underlying business logic — every existing store action, Server Action, and data flow is untouched.

**Cross-app design/motion/accessibility audit (done as a follow-up pass).** Grep-driven, not screen-by-screen guesswork — every `rounded-*`, `duration:`/`ease:`, `focus:outline-none`, and `aria-label` occurrence across `app/` and `components/` was inventoried and compared. Findings and fixes:
- **Zero visible keyboard focus indicator, anywhere.** Every text input/textarea/select in the app used Tailwind's `focus:outline-none` with nothing replacing it — `focus-visible`/`focus:ring` appeared nowhere in the codebase. A real WCAG 2.4.7 gap, not a style nitpick: a keyboard-only user could not tell which field was focused on any screen. Fixed with one shared `.focus-ring` utility (`app/globals.css`) applied at all 9 call sites — `:focus-visible` only, so it never flashes on a mouse click.
- **Three icon-only buttons had no padding at all** (`NavBar`'s sign-out, `AICompanion`'s close, `GoalsPanel`'s delete-goal) — their clickable area was the bare icon's bounding box, below WCAG 2.5.8's 24×24px minimum. Given `p-1.5`/`p-2` + a subtle hover background, matching the affordance every other interactive element in the app already has.
- **No `prefers-reduced-motion` support anywhere.** Fixed once, app-wide, via `<MotionConfig reducedMotion="user">` wrapping the root layout — every existing and future `framer-motion` animation now respects the OS-level setting automatically, rather than each component needing its own check.
- **Two real inconsistencies, both introduced by Experience Layer v1 itself:** `AIBriefing`'s skeleton used `rounded-md`, the only occurrence of that radius anywhere in the app (the established scale is `full`/`lg`/`xl`/`2xl` only) — changed to `rounded-full` to match the other thin-bar elements. Seven `motion.li`/`motion.div`/`motion.h1` transitions across `AreaMomentsView`, `OnboardingFlow`, `GoalsPanel`, `app/page.tsx`, `app/areas/page.tsx`, `app/areas/family/page.tsx`, and `app/timeline/page.tsx` omitted an explicit `ease: "easeOut"` (falling back to framer-motion's default curve, subtly different from the rest of the app) — all seven now specify it explicitly.
- **Spot-checked, not instrumented, contrast:** `text-muted` (`#a1a1aa`) against the app's near-black background (`#0a0a0b`) computes to roughly 7.5:1 — comfortably above WCAG AA's 4.5:1 for normal text. A full axe/Lighthouse contrast sweep wasn't run (no live authenticated session to point it at — see the standing browser-verification gap in `docs/BACKLOG.md`).

**Goals Experience v2 (second Experience Layer screen, after Timeline).** `GoalsPanel` graduated from a flat CRUD list to an AI-guided journey — every goal now surfaces stage, progress, completed milestones, a Recommendation-Intelligence-tracked next action with its rationale, a real estimated completion, and related memory/knowledge, instead of just a progress bar and a checklist.

`app/api/goals/insights` (new route) computes this per goal, on every request, from a fresh canonical read (`goalsRepo.listWithMilestones`) — not the client's cached copy, same reasoning as calendar suggestions' server-authoritative scores (§5). Per goal:
- **Stage** (`lib/goals/deriveGoalStage.ts`, pure, unit-tested) — `starting` / `in_progress` / `stuck` / `completed`, deterministic from milestone completion and age. "Stuck" reuses Personal DNA's `goalMomentum` analyzer's own `STAGNATION_DAYS` constant (now exported from `lib/intelligence/personalDNA/analyzers/goals.ts`) rather than a second number that means the same thing — one goal's stage badge and the aggregate cross-goal pattern now agree on what "stagnant" means by construction.
- **Estimated completion** (`lib/goals/estimateCompletion.ts`, pure, unit-tested) — remaining milestones × the user's own real `milestoneCompletionPace` (Personal DNA, §3), read as a raw pattern row (`personalPatternsRepo.list`, not the pre-formatted description string `getPersonalPatternDescriptions` returns) and gated on the same `MIN_CONFIDENCE_TO_SURFACE` threshold every other consumer of `personal_patterns` uses. No pattern confident enough yet → no estimate shown, never a guessed default.
- **Next recommended action** — the next incomplete milestone (already meaningfully ordered from goal-breakdown time), given a real rationale (`lib/goals/buildNextActionRationale.ts`, pure, unit-tested, prioritized: stuck → weakest life area → related memory → task-size preference → honest structural fallback) and a real confidence number reusing `lib/suggestionConfidence.ts`'s `computeSuggestionConfidence` — the identical "how far does this life area lag the average" signal calendar suggestions' confidence bar already expresses, not a second invented number.
- **Related memory** — `retrieveRelevantMemory` (Memory Engine, §2) queried with the goal's own title, the same pattern `/api/goals/breakdown` already established.
- **Recommendation Intelligence integration, done properly for this surface's lifecycle:** unlike a calendar suggestion (genuinely new each request), the same milestone stays "next" across repeat page views — so the route reuses an existing `pending` `goal_next_action` recommendation_event for that goal instead of creating a fresh one every load. "Accept" *is* completing the milestone (`toggleMilestone` + `recordRecommendationOutcome("accepted")`, the same Server Action calendar suggestions already call) — there's no separate "I intend to" state. "Dismiss" records `rejected` and hides the card locally, mirroring `ScheduleSuggestions`' own dismiss behavior.

**A deliberate non-use of `buildAtlasContext`, stated as plainly as Timeline's own honest limitations (§6):** this route does not call the Context Engine's composed function. `buildAtlasContext` always fetches goals/life-areas/upcoming-events/people together and returns personal patterns as pre-formatted prose — this route needs its own fresh goal read regardless, the *raw* pattern values (arithmetic, not prose), and a different memory query per goal. Calling it would mean paying for several fields every request that this route immediately discards. Instead the route calls the same underlying repos and functions the Context Engine itself is composed from (`personalPatternsRepo`, `lifeAreaScoresRepo`, `retrieveRelevantMemory`) directly — genuine reuse of the data-access layer, not a pass-through of a bundle shaped for a different job.

**UI.** `GoalJourneyCard` (new component) renders instantly from the live store (title, category, progress bar, full milestone checklist — nothing lost from the old list) and layers the insight in once `/api/goals/insights` resolves, the same progressive-enhancement convention `AIBriefing` established — a pulsing skeleton stands in for the stage/next-action block until then. No new design tokens: stage icons/colors, the confidence bar, and the accept/dismiss buttons all reuse existing accent colors, `GlassCard`, and the exact `ScheduleSuggestions` confidence-bar markup.

**Deliberately not built this milestone:** re-ranking *which* milestone is "next" by anything beyond stored order — no milestone carries effort/size metadata to rank by, so inventing a scoring algorithm over them would have been speculative, not real intelligence; a "modify" interaction (same reasoning Experience Layer v1 already documented — no real modify flow exists anywhere yet); feeding `estimatedCompletionDays`/stage back into Personal DNA's confidence model (a real future coupling, same "needs real volume to design deliberately" gate as §7's own deferred items).

**Learning Experience v2 (third Experience Layer screen, after Timeline and Goals) — Torah Space redesigned into an AI learning workspace.** "Learning" in the product vision maps to `app/areas/torah` in the actual codebase — it's the only real upload area (`knowledge_entries`); the separate `/areas/learning` page is a generic knowledge-category moments log with no upload path and is untouched by this milestone. The Torah page graduated from a flat "סדר בוקר" list to a personal knowledge library: every session now surfaces a study streak, real learning progress, a Recommendation-Intelligence-tracked suggested next review, and — per entry — related knowledge, related memory, connected goals, and on-demand flashcards/review questions.

**No new AI provider was introduced, per explicit instruction.** Flashcards and review questions (`app/api/torah/study-material`, new route) reuse the exact `generateObject` + `gpt-4o-mini` pipeline `app/api/torah/extract` already established — a different zod schema over the same already-extracted summary/topic/source, not a new capability. Generated lazily (only when a user opens "תרגל" on a specific entry) and cached in two new nullable `knowledge_entries` columns (`flashcards`, `review_questions` — migration `20260720000006`) so a repeat visit costs nothing and calls no AI at all. Honest fallback, unchanged from every other AI route in this app: no `OPENAI_API_KEY` → an explicit "not connected" message, never fabricated cards.

`app/api/torah/insights` (new route) computes, per request, from a fresh canonical read:
- **Study streak** (`lib/learning/computeStudyStreak.ts`, pure, unit-tested) — consecutive local-calendar days with a logged session, bucketed via `getLocalDateKey` (newly exported from `lib/intelligence/personalDNA/timezone.ts`, alongside `getLocalHour`/`getLocalDayOfWeek` — `analyzers/routine.ts` now imports the same function instead of its own private copy, so a goal's routine pattern and a learning streak agree on what "the same day" means by construction).
- **Learning progress** — Personal DNA's `learningTopicFocus`/`learningCadence` patterns (§3), read as raw rows the same way Goals Experience v2 reads `milestoneCompletionPace` — real numbers, gated on `MIN_CONFIDENCE_TO_SURFACE`, never shown until there's real evidence.
- **Suggested next review** (`lib/learning/pickNextReview.ts`, pure, unit-tested) — the entry touched least recently (`lastReviewedAt ?? date`), a real, explainable, non-invented signal — deliberately not a spaced-repetition curve with parameters nothing has validated. Tracked as a `pending` `learning_next_review` recommendation_event, deduplicated across page views exactly like Goals' `goal_next_action` (the same reason: the same entry stays "due" until acted on, unlike a calendar slot). "Accept" *is* marking it reviewed (`markKnowledgeReviewed` + `recordRecommendationOutcome("accepted")`); "dismiss" records `rejected` and hides the card.
- **Related knowledge and related memory, genuinely distinguished, not two names for the same call:** related memory reuses `retrieveRelevantMemory` (Memory Engine, §2) as-is, spanning moments/knowledge/insights together. Related knowledge calls `rankByRelevance` (the same underlying ranking function Memory Engine already exports) directly, scoped to just the *other* knowledge entries — one algorithm, two candidate sets, not two ranking implementations.
- **Connected goals** — `tokenize` (Memory Engine's own tokenizer, reused directly) checks a knowledge entry's topic against active goal titles for real keyword overlap, surfaced as a badge. No new schema relationship (no `goal_id` foreign key) — a shiur can relate to zero, one, or several goals, and a rigid FK would force a 1:1 link reality doesn't have.
- **Connection to Timeline, already real, nothing new needed:** Timeline Experience v1 (§6) already turns every `knowledge_entries` row into a `knowledge_session` timeline event — marking an entry reviewed or generating study material doesn't change that mapping, so the "connect learning items to Timeline" requirement was already satisfied before this milestone started.

**Same deliberate non-use of `buildAtlasContext` as Goals Experience v2, for the same reason:** this route needs its own fresh `knowledge_entries` read, raw pattern values, and a different memory/relevance query per entry — none of which fits the Context Engine's single pre-formatted bundle. It calls the same underlying repos and functions (`personalPatternsRepo`, `retrieveRelevantMemory`, `rankByRelevance`, `tokenize`) directly.

**UI.** `LearningInsightsHero` (new component) mirrors `AIBriefing`'s progressive-enhancement and quiet-empty-state conventions exactly — a pulsing skeleton while insights load, nothing rendered at all if there's genuinely no streak, no confident pattern, and no next review. `KnowledgeLibraryCard` (new component) renders the existing topic/source/summary card unchanged and adds an expandable study section, related-knowledge/memory block, and connected-goal badges — same `GlassCard`, accent colors, and confidence-bar markup as every other card in the app, no new design tokens. A plain client-side search filter (topic/summary substring) makes the library feel like a library rather than a flat file list, without inventing a tagging system.

**Deliberately not built this milestone:** persisting flashcard review state (spaced-repetition scheduling per card) — "suggested next review" operates at the entry level, which is what the real data (`last_reviewed_at`) supports; per-card scheduling would need per-card review history nothing collects yet; a true spaced-repetition algorithm (SM-2 or similar) — the confidence-weighted recency signal here is honest about being a simpler, real heuristic, not a curve tuned against data that doesn't exist; feeding learning streak/progress back into Personal DNA's confidence model (same "needs real volume" gate as every other cross-engine coupling deferred in §7 and above).

**Areas Experience v2 (fourth Experience Layer screen) — the areas hub becomes the Life Dashboard.** `app/areas` graduated from a 5-card navigation grid (icon, score, progress bar, link) to a central map: every card now surfaces an attention level (healthy/growing/needs attention), active-goal count, a real activity trend, one prioritized highlight (recommended action, behavioral insight, or recent memory), and the grid itself is ordered by what actually needs looking at — not a fixed enum order.

**Fully reusable, by construction, not by promise.** `AreaHealthCard` (new component) and `app/api/areas/insights` (new route) are entirely data-driven — keyed off `LIFE_AREA_LIST`, with zero per-area special-casing beyond the one honest exception already established elsewhere (Torah/`faith` folding in `knowledge_entries`, same as Timeline and Learning Experience v2). The moment Health has a real write path, or a sixth life area (Finance) is added to `lib/lifeAreas.ts`, both the card and the route work for it automatically — nothing about this milestone is faith/family/knowledge/health/career-specific code.

`app/api/areas/insights` computes, per area, from a fresh canonical read:
- **Attention level** (`lib/areas/deriveAttentionLevel.ts`, pure, unit-tested) — reuses the Intelligence Engine's own `WEAK_LIFE_AREA_SCORE_THRESHOLD` (§9) for "needs attention," rather than a second number that means the same thing; a stuck goal in that area (reusing `deriveGoalStage`, Goals Experience v2) or activity that's fully stopped overrides a merely decent score.
- **Recent progress** (`lib/utils.ts`'s `computeActivityTrend`, pure, unit-tested — originally written here, relocated out of `lib/areas/` once Family Experience v2 needed the identical windowed-count logic for a second domain) — there's no score-history table (§8), so this is an honest windowed-count proxy (last 14 days vs. the 14 before that) over `moments`/`knowledge_entries` timestamps that already exist, not a fabricated trend line.
- **Active goals** — a real count from a fresh `goalsRepo.listWithMilestones` read, filtered by category.
- **Recommended next action, read-only** — when an active goal exists in the area (the stuck one wins if there is one, else the least-progressed), its next milestone is surfaced using Goals Experience v2's own `deriveGoalStage`/`buildNextActionRationale` directly — not reimplemented. Deliberately **not** tracked as a second `recommendation_event`: the same suggestion is already the tracked `goal_next_action` on the Goals panel, and giving the Life Dashboard its own accept/dismiss controls for the identical decision would be a second control surface for one choice, exactly the "dashboard full of widgets" this milestone was told to avoid. Accept/dismiss stays where the actionable milestone lives.
- **AI insight** — Personal DNA's `focus` analyzer already produces one pattern per life area (`category: "focus"`, `subject` = the area key) — read raw, confidence-gated, surfaced as-is. No new analyzer, no new pattern type.
- **Recent-activity highlight** — Memory Engine (`retrieveRelevantMemory`) queried with the area's own label, the same per-item pattern Goals/Learning Experience v2 established, now applied to a life area instead of a goal or a knowledge entry.
- **Grid order, a genuine (if narrow) Intelligence Engine reuse** — a small, real `IntelligenceSignal` pair per area (the life-area score signal + the focus-pattern signal, when one exists), built with the engine's own exported `CATEGORY_DEFAULTS`/`WEAK_LIFE_AREA_IMPORTANCE` (§9, now exported for this reuse) and ranked with the real, unmodified `rankSignals` — not `buildIntelligenceSignals`/`buildAtlasContext`, which need a full `AtlasContext` this route doesn't have and would double-fetch goals/patterns it already reads directly. This is `rankSignals`'s formula reused for something new — a fifth real call site for the ranking primitive itself, short of §9's own "fifth AI-backed surface" bar for the full pipeline, which stays unmet.

**Same deliberate non-use of `buildAtlasContext` as Goals/Learning Experience v2, and for the first time seriously reconsidered and still declined:** life-area signals are the *most* natural fit `buildIntelligenceSignals` has (`lifeArea` is a native category), which made this worth weighing carefully — but this route needs raw goal rows for per-category counts and priority-goal selection regardless, so calling `buildAtlasContext` on top would mean fetching goals and patterns a second time, an actual duplicate query, not just an unused field. It calls the same underlying repos directly instead.

**UI.** `AreaHealthCard` shows exactly four lines — header with attention badge, score/progress bar, a compact stats line, one prioritized highlight (never more than one, chosen recommended-action → AI-insight → recent-memory → nothing) — deliberately not a stat-tile grid or a sparkline chart. Reuses `GlassCard`, existing accent colors, and the same progressive-enhancement/skeleton convention as every other Experience Layer screen; no new design tokens.

**Deliberately not built this milestone:** a real score-history table (the activity-trend proxy above is the honest v1 substitute — §8's "nothing speculative ahead of need" applies here exactly as it did to Memory Engine's own versioning); animated grid-reorder transitions (the one-time reflow when insights load is plain, matching "avoid speculative architecture" over polishing a rare, one-time layout shift); a second `recommendation_event` type for area-level nudges (see "read-only" above); feeding activity trend back into Personal DNA's confidence model (same "needs real volume" gate as every other cross-engine coupling already deferred).

**AI Companion Experience v2 — the chat panel becomes the primary intelligence surface, not a generic chat box.** Two real, non-speculative changes carry this milestone, both reusing infrastructure that already existed rather than adding any:

**1. A proactive opener, not a blank greeting.** `AICompanion`'s empty state (no conversation yet) now fetches `/api/briefing` — the exact same route and Intelligence Engine output Today's `AIBriefing` already renders — and shows it via a newly shared `BriefingSignalList` component (extracted from `AIBriefing`, which now calls it too; one rendering, two consumers, not a duplicate). This directly answers "proactively surface important context when appropriate" and "connect goals, memories, calendar, learning, and life areas without the user asking" with zero new intelligence: the briefing already spans all eight signal categories. It costs no extra AI call (the briefing pipeline is deterministic, not an LLM call) and it is not background automation — it only fires when the user opens the panel, same as every other on-demand fetch in this app. Quick-action chips below the signals are derived from the same real data (`ספר לי עוד על: {signal.summary}`) and only *fill* the input, never auto-send — consistent with the app's standing rule that Atlas never acts without an explicit user action. The opener disappears the moment a real conversation exists, so it never repeats itself alongside live context — the same "avoid repeating information already known" instruction, honored structurally rather than by prompting alone.

**2. Streaming, and a real "based on" instead of a black box.** `app/api/chat` switched from `generateText` to `streamText` — the same `ai` SDK, same `gpt-4o-mini`, no new provider or infrastructure, just the SDK's own streaming capability (`toTextStreamResponse`). `lib/chatSystemPrompt.ts`'s `buildSystemPrompt` now returns `{ prompt, topSignals }` instead of a bare string — the ranked signals it already computed to build the prompt, not a second pass — and the route surfaces the top few as an `x-atlas-based-on` response header. `AICompanion` reads it and renders a collapsed "מבוסס על" toggle under each assistant reply — this milestone's concrete answer to "explain why it is making recommendations" and "clearer reasoning sections," grounded in the literal signals used, never a fabricated explanation. This is kept deliberately ephemeral (session-only, keyed by the client-generated message id, never written to `chat_messages`) rather than a new persisted column — a real product feature that didn't need a schema change to exist.

**Continuity, addressed at the root cause, not papered over with a bigger prompt.** `lib/memory/retrieveRelevantMemory` (Memory Engine, §2) now also ranks `chat_messages` alongside moments/knowledge/insights — real support for "reference previous conversations naturally" and "maintain conversational continuity across sessions," since a conversation beyond the 50-message cap `/api/chat`'s own zod schema enforces can no longer rely on raw history alone. The base system prompt also gained explicit instructions — don't restate what's already been said this conversation, explain rationale briefly and only when grounded in real context, weave in continuity naturally rather than treating each message in isolation — a prompt-level change, not a new tracking mechanism, for exactly the qualities that don't need one.

**Deliberately not built this milestone, per explicit instruction:** any autonomous or scheduled companion behavior (no cron, no unprompted push — the proactive opener is still request-triggered); a second AI provider (Gemini or otherwise) for anything here — every capability above runs on the existing OpenAI integration; persisting per-message "based on" data (would need a `chat_messages` schema change for a transparency feature that works today without one); de-duplicating context already shown on *other* screens (e.g., not repeating something Today's `AIBriefing` already surfaced) — that would need cross-surface "what has this user already seen" state tracking that doesn't exist anywhere in the app yet, and inventing it now would be exactly the speculative infrastructure this milestone was told to avoid.

**A known, honest trade-off of adopting streaming:** the previous `generateText` implementation could `try/catch` a genuine OpenAI API failure (bad key, outage) and fall back to the honest mock reply entirely server-side, before any response left the server. `streamText` commits to a response before the model call fully resolves, so a failure *after* streaming has started surfaces to the client mid-stream rather than as a clean server-side fallback — `AICompanion`'s stream-reading loop catches this and shows the same friendly error message as before, just one layer further out. The `OPENAI_API_KEY` unset case and any error before `streamText` is called (e.g. `buildAtlasContext` failing) are still caught server-side exactly as before.

**Family Experience v2 — Family Care becomes a relationship workspace, not a contacts list.** Audited first, per explicit instruction: the real gap wasn't missing intelligence, it was a missing structural link. `moments` had no way to say *which person* a family moment was about — "log a moment" wrote a person's name into free-text `title`/`content` (`"רגע עם {name}"`) and nothing else connected them. Migration `20260720000007` adds `moments.person_id` (nullable FK, `on delete set null`) — a real, minimal schema change that turns "relationship timeline" and "interaction history" from a name-matching guess into an actual query.

**A second real gap the audit found: the same rule, implemented twice.** `lib/context/buildAtlasContext.ts`'s `relationshipSignals` and the Family page itself each independently computed "is this person stale" (`daysSince(...) >= staleThresholdDays`) — same condition, two inline copies. `lib/family/deriveRelationshipHealth.ts` now exports `isPersonStale`, and `buildAtlasContext.ts` was refactored to call it instead of its own copy — one source of truth, and this *is* requirement 9 (integrate with Today): the existing `relationshipSignals` → chat/`AIBriefing` pipeline needed no new wiring, just a shared, more honest predicate feeding it.

`app/api/family/insights` (new route) computes, per person, from a fresh canonical read:
- **Relationship health** (`lib/family/deriveRelationshipHealth.ts`, pure, unit-tested) — healthy/growing/needs_attention, mirroring `lib/areas/deriveAttentionLevel.ts`'s exact three-tier shape and reasoning (no history yet is neutral, not urgent; past the user's own stale threshold always needs attention; a declining interaction trend keeps a recently-touched relationship from reading as fully healthy).
- **Interaction trend** — `lib/utils.ts`'s `computeActivityTrend`, relocated out of `lib/areas/` this milestone once a second domain needed the identical windowed-count logic, over this person's own `person_id`-tagged moments.
- **Relationship timeline** — `lib/timeline/buildTimelineEvents.ts` (Timeline Experience v1, §6) called as-is against a person-scoped moment list. No second timeline implementation — the module was already generic enough.
- **Related memory** — `retrieveRelevantMemory` (Memory Engine, §2) queried with the person's own name, the same per-item pattern every prior v2 milestone established.
- **Suggested next interaction** (`lib/family/pickSuggestedAction.ts`, pure, unit-tested) — call/message/meet/congratulate, chosen deterministically from real signals (health, an imminent birthday, whether real shared history exists), never LLM-generated — the same discipline Goals' next-milestone and Learning's next-review already follow. **"Pray" was deliberately not included**: there is no real signal (health/wellbeing data) behind it for any person, and inventing one would be the first fabricated insight anywhere in this app's intelligence layer.
- **Confidence** — `lib/suggestionConfidence.ts`'s `computeSuggestionConfidence`, reused wholesale: a per-person recency score (0–100, relative to their own stale threshold) plays the same role a life area's score plays for calendar suggestions. No second confidence formula.
- **Priority order across people** — a real per-person `relationship`-category `IntelligenceSignal`, built with the engine's own `CATEGORY_DEFAULTS` and ranked with the unmodified `rankSignals`, the same "let the engine decide order" reuse Areas Experience v2 established for its grid.
- **Recommendation Intelligence** — a new `family_next_interaction` event type, deduplicated per person exactly like `goal_next_action`/`learning_next_review` (the same person stays "due" across page views, unlike a calendar slot). "Accept" (`עשיתי את זה`) logs a real interaction — reusing the existing `logPersonInteraction`/`addMoment` flow, now tagged with the real `person_id` — and records `accepted`. "Dismiss" records `rejected` and hides the suggestion locally, mirroring every prior v2 milestone's accept/dismiss pattern. A separate, always-available "רשום רגע איתם" button remains for logging an interaction that wasn't the suggested one.

**Same deliberate non-use of `buildAtlasContext` as every prior v2 milestone, for the same reason:** this route needs a fresh, person-scoped `moments` read, raw values for arithmetic, and a per-person memory query — none of which fit the Context Engine's single pre-formatted bundle. It calls the same underlying repos directly, including the one predicate now genuinely shared with `buildAtlasContext.ts` itself.

**UI.** `PersonRelationshipCard` (new, reusable) renders name/relation/note/birthday instantly from the live store (unchanged) and layers in the health badge, interaction stats, and suggested action once insights resolve — the same progressive-enhancement convention every other screen uses. The relationship timeline and related memory are folded into one collapsed "הצג היסטוריית קשר" toggle rather than always-visible sections, keeping the base card as compact as `AreaHealthCard`'s four lines. No new design tokens.

**Deliberately not built this milestone:** any literal call/message/meeting integration (telephony, SMS, calendar invites) — "accept" logs the interaction as having happened, it doesn't place a call, and building toward that would be new infrastructure, not a product experience improvement; a `person_id` FK on `knowledge_entries` or `insights` (only `moments` needed it for this milestone's real use case); feeding relationship health back into Personal DNA's confidence model (same "needs real volume" gate as every other cross-engine coupling already deferred).

**Explicitly still deferred** (audited and named, not silently skipped): a skip-to-content link (low priority given `NavBar` has only 3 short links); Lighthouse performance numbers and a full manual screen-reader pass (both need a live authenticated session this environment can't produce); the generic `/areas/learning` page (still a plain moments log, distinct from the Torah/Learning workspace — the one screen left from the original Experience Layer audit); visual-intelligence indicators beyond what `AIBriefing`, `GoalJourneyCard`, `LearningInsightsHero`, `AreaHealthCard`, and now `PersonRelationshipCard` each already show; any UI for the recommendation-feedback `modified` status, which needs a real edit flow to exist first.

**`Modal` focus trap + return-focus-on-close (follow-up fix, after Family Experience v2).** The one concrete a11y gap this document had been naming and deliberately not attempting — "risks breaking keyboard nav worse than the current gap without a live browser to test against" — implemented directly, using the standard WCAG modal-dialog pattern rather than a new dependency: on open, focus moves into the panel unless something inside it already claimed it (so `QuickCapture`'s and `OnboardingFlow`'s existing `autoFocus` fields keep working unchanged — the effect checks `panel.contains(document.activeElement)` before acting); `Tab`/`Shift+Tab` wraps within the panel's own focusable elements while open; on close, focus returns to whatever element held it beforehand (typically the button that opened the modal, captured via `document.activeElement` at open time — no new prop threading from each of the three call sites). `role="dialog"`/`aria-modal="true"` were added to the panel at the same time, and the panel itself now carries the app's own `.focus-ring` utility (§10's own earlier fix) rather than a bare `focus:outline-none`, so the one case where the panel itself receives focus (no focusable child exists) still shows a visible indicator. All three usages (`AICompanion`, `QuickCapture`, `OnboardingFlow`) share one implementation in `components/ui/Modal.tsx` — no per-consumer changes needed. Verified via typecheck, lint, the full test suite, and a production build; **not** verified against a live authenticated session (every modal in this app renders behind real Google sign-in, which this environment cannot drive) — the same standing caveat as every other browser-verification gap in this document, not a new one.

**Add-a-person, closing a real foundational gap (follow-up fix, same session as the Modal fix, 2026-07-22).** An audit prompted by a request to deepen onboarding's family-capture step found something more basic missing underneath it: there was no way to add a new family member anywhere in the app. `app/actions/people.ts` only ever exposed updates to an existing row (`logPersonInteractionAction`, `setPersonBirthdayAction`) — every `people` row in the live database had been seeded directly, never created through the product. `peopleRepo.insert` (the generic user-scoped repo's own `insert`, `lib/db/createUserScopedRepo.ts`) already existed and needed no change; the gap was purely the missing Server Action and UI. `addPersonAction` (new, `app/actions/people.ts`) and a matching `addPerson` store action follow the exact same shape as `addGoalAction`/`addGoal`. The Family page's own new inline "add person" row (name + relation, optional birthday deliberately left for the existing per-card `הוסף יום הולדת` flow rather than duplicating it) mirrors `GoalsPanel`'s established inline-add-row convention exactly, not a new modal — `QuickCapture`'s modal pattern is for a global floating action, this is a page-scoped one, and the goals panel is the closer precedent. Verified via typecheck, lint, the full test suite, and a production build, plus a direct live-database smoke test (a throwaway user, inserted and cleaned up the same way `scripts/verify-phase1.mjs` already does) confirming the exact insert shape this action sends satisfies the real `people` table's schema and constraints. Not yet wired into onboarding itself — that's the next step this unblocks, not part of this fix.

**Also this session: Supabase migrations confirmed applied, live isolation re-verified.** This environment now has real outbound network access to Supabase, which it didn't in earlier sessions. `npm run db:migrate` confirmed both previously-pending migrations (`20260720000006`, `20260720000007`) were already applied; `scripts/verify-phase1.mjs` re-run live, 12/12 checks passed. Two stale "not yet applied" entries removed from `docs/BACKLOG.md` accordingly — not new work, a confirmation that Family Experience v2 and Learning Experience v2's study-streak/flashcard features have been reflecting real data all along.

**Trigger for the next increment:** every screen named in the original Experience Layer audit (Today, Timeline, Goals, Learning, Areas, the Companion, Family) has now been through this pass except the generic `/areas/learning` moments log — real usage data, not top-to-bottom assumption, should decide whether that's worth its own redesign. The next real increment taken (§11) was a cross-cutting one instead: Deep Onboarding, feeding real conversational data into the Personal DNA table for the first time.

---

## 11. Deep Onboarding — a real conversation, not a form

**What it is:** first-run setup graduated from a fixed 3-question list (`ONBOARDING_QUESTIONS`, `lib/constants.ts`) to an actual AI conversation covering five real topics — family, career, daily habits, sleep, and peak focus hours — that asks one natural question at a time in Hebrew, extracts structured data from each answer, and writes it straight into `personal_dna` and `people` as the conversation happens, not only at the end.

**The one deliberate trust boundary, stated up front:** the model's own reply is never the authority on whether onboarding is done. `lib/onboarding/deepOnboarding.ts`'s `isOnboardingComplete` only ever looks at the real, already-merged `PersonalDNA`/`people` state (a topic counts as covered once its real field is actually non-empty, or the user explicitly asked to skip it) plus a hard `MAX_ONBOARDING_TURNS` (10) backstop. The model can phrase things however it likes, combine an acknowledgment with the next question, or wander — it cannot talk its way into ending the conversation early, nor keep it open forever. This mirrors the same discipline every other AI-backed route in this app already follows (goal breakdown's parsed milestones, Torah's extraction pipeline): the model proposes, deterministic code decides.

**Schema.** New migration `20260720000008_personal_dna_deep_onboarding.sql` adds `sleep_notes`, `career_notes`, and `motivation_triggers` (a real accumulating list, kept separate from `habit_notes` — a habit is something you *do*, a motivation trigger is *why* you do it, and conflating them would make both fields mean less) to `personal_dna`. `family` doesn't get a new column at all — it's satisfied by the `people` table Family Experience v2 already owns, via the `people` count this milestone's route reads directly.

**`app/api/onboarding/message`, the thin orchestrator.** Same shape as every other AI-backed route (`getServerSession` → 401, `rateLimitResponse`, `parseJsonBody` + zod, `isProviderConfigured` honest fallback, try/catch around the model call): reads the user's real current `personal_dna` + `people` count, asks `lib/onboarding/deepOnboarding.ts`'s pure `deriveCoveredTopics` which of the five topics are still open, builds a system prompt (`buildOnboardingSystemPrompt`) that tells the model exactly what's left and how many exchanges remain, calls `generateStructuredData` (the existing Unified AI Provider Layer, no new model or provider) with a zod schema (`OnboardingExtractionSchema`) covering `reply` (the next Hebrew message), `extracted` (whatever structured fields the user's last answer actually contained), and `topicsSkipped` (if the user asked to move on). It then **applies** what was extracted immediately — `peopleRepo.insert` for any new family member mentioned, `personalDnaRepo.upsert` for the merged patch (`mergePersonalDnaPatch`: array fields accumulate and dedupe, scalar fields only overwrite when the model actually extracted something new this turn, never clobbering an earlier answer with an empty one) — before recomputing `deriveCoveredTopics` against the *post-write* state to decide `complete`. This is the literal meaning of "continuously update a user profile," turn by turn, not a batch job at the end.

**No new schema for "skip."** Which topics a user chose to skip is real but ephemeral — carried in the request/response payload (`skippedTopics`) the same way chat's "based on" reasoning is deliberately session-only (§10) rather than persisted, since nothing downstream needs to remember *that a topic was skipped*, only the real data that resulted (or didn't).

**Honest fallback, same pattern as every other AI route.** No `OPENAI_API_KEY` → the route returns `fallbackToStaticForm: true` instead of a fabricated conversation; `OnboardingFlow` (`components/features/OnboardingFlow.tsx`) renders `DeepOnboardingChat` first and swaps to the original static question list (renamed `StaticOnboardingForm`, otherwise byte-for-byte unchanged) the moment that flag comes back. The one flow that must always work — first-run setup — never depends on a live model call being configured correctly.

**UI.** `DeepOnboardingChat` (new) is a minimal chat surface inside the same non-dismissable `Modal` (`Z_INDEX.onboarding`) onboarding already used — no new design tokens, same `focus-ring`/accent-color conventions as `AICompanion`. It renders turns and sends the next one; it does not decide what to ask, what was learned, or when to stop — all of that is the route's job, kept there so it stays server-authoritative and testable independent of any UI.

**Verified via** typecheck, lint, the full test suite (16 new unit tests on the pure `lib/onboarding/deepOnboarding.ts` logic — topic coverage, the turn-cap backstop, patch merging/deduping, birthday-format validation, prompt construction), a production build, `npm run db:migrate` against live Supabase, and a direct live-database smoke test (throwaway user) confirming the upsert-merge semantics (a later turn's fields don't clobber an earlier turn's) hold against the real schema. **Not verified:** an actual end-to-end conversation through a live authenticated browser session, nor against a real `OPENAI_API_KEY` (unset in this environment) — same standing caveat as every other AI-backed feature and every other live-session gap already named in this document, not a new one. The existing static form remains the load-bearing path until both are available.

**Deliberately not built this milestone:** `learningStyle` was kept in the extraction schema (existing consumers — chat's system prompt, calendar suggestion rationale — already depend on it) but is not one of the five gated topics and isn't explicitly prompted for; the model may pick it up opportunistically if a user mentions it, nothing more. A goal-capture step was considered (an earlier draft of this plan suggested it) but dropped from this milestone's explicit scope of exactly five topics — could be a natural follow-up once onboarding's own five topics have real usage behind them. Feeding the new `career_notes`/`sleep_notes`/`motivation_triggers` fields into the *behavioral* Personal DNA Engine (§3) or the Intelligence Engine's ranking (§9) is a real future coupling, not attempted here — those engines infer patterns from usage; these are stated answers, and conflating "what the user said once" with "what the engine has observed repeatedly" would blur a distinction this app has been careful to keep everywhere else.

---

## 12. Today Dashboard v2 & the AI Command Panel

**The 3-layer navigation (Today / Areas / Timeline) already existed** — `NavBar` (`components/layout/NavBar.tsx`) has linked exactly these three since Timeline Experience v1 (§6) and Areas Experience v2 (§10), and `AICompanion` has been a persistent floating panel, not a fourth nav tab, since it was built. Audited before touching anything: a floating action button for an omnipresent assistant is a deliberate, still-correct choice, not a gap — this milestone added real capability to that existing shell rather than rebuilding it.

**Energy level, a real signal from the Personal DNA Engine, not a decoration.** `app/api/today/energy` (new) compares the current real local hour against the user's own confidence-gated `peakActivityWindow` pattern per life area (§3, `lib/intelligence/personalDNA/analyzers/focus.ts` — `hourToWindow`/`WINDOW_LABELS` exported from there rather than re-implemented, so "evening" means the exact same window everywhere it's used). `lib/energy/deriveEnergyLevel.ts` (pure, unit-tested) returns one of three honest states: `unknown` (no confident pattern yet — renders nothing, the same quiet-empty-state convention every Experience Layer surface follows), `typical` (this hour isn't a known peak for any area — deliberately *not* labeled "low energy," since absence of a positive match is not evidence of anything negative), or `peak` (a real, named match). `EnergyLevelBadge` (new, `components/features/EnergyLevelBadge.tsx`) renders it as one quiet line under the greeting — no new dashboard widget grid.

**Screen Time, honestly labeled for what it actually measures.** `ScreenTimeWidget` (new) is entirely client-side (`localStorage`, no schema, no server round-trip) and only ticks while the Atlas tab is actually visible and focused — a background tab left open all day doesn't inflate it. Deliberately never called "screen time" in the UI copy (`X דקות באטלס היום`, "X minutes *in Atlas* today"): a web app has no access to real OS/device screen time, and claiming to measure it would be exactly the fabricated-signal problem this app's Experience Layer has avoided everywhere else.

**The AI Command Panel — free-text Hebrew commands that turn into a real, confirmable action.** `app/api/commands/interpret` (new) classifies a command into exactly five outcomes via `generateStructuredData` (existing Unified AI Provider Layer, no new model): `add_moment`, `add_goal`, `log_family_interaction`, `clear_calendar_range`, or `unclear` — a deliberately bounded set, not a general agent over arbitrary app state. **The one deliberate trust boundary, stated up front, same discipline as Deep Onboarding (§11) and every other AI route in this app:** the model proposes; it never executes. Every outcome returns a `proposal` the user must explicitly confirm before anything is mutated — the app's own standing rule ("Atlas never acts without an explicit user action," first stated in §10's AI Companion Experience v2) applies to commands exactly as it does to every other suggestion surface. Confirming `add_moment`/`add_goal`/`log_family_interaction` calls the exact same already-tested store actions every other part of the app uses for the same mutation; nothing new was built for those three beyond the interpretation step.

**`clear_calendar_range` is the one outcome that needed a real new capability**, since "clear my evening" only means something against the user's actual Google Calendar, which Atlas had never had delete access to before (`app/api/calendar/events` only ever supported creating an event). The route resolves "evening"/"tomorrow"/etc. into a real UTC time window (`lib/commands/timeWindow.ts`) — a genuinely tricky piece, since the server process doesn't run in Israel's timezone and Israel observes DST (UTC+2 in winter, UTC+3 in summer): the offset is read live via `Intl.DateTimeFormat`'s `shortOffset`, never hardcoded, and covered by unit tests spanning both a winter and a summer date plus a midnight/month-rollover case for the "night" period. It then calls Google's real `events.list` for that window and returns the *actual* matching events (title, id, time) as the proposal — never a guess at what might be there. A new `DELETE` handler on `app/api/calendar/events` (mirroring the existing `POST`'s auth/token pattern exactly) is the only thing that actually removes an event, and only once the user has seen the real list and confirmed.

**Family-name resolution never guesses.** `lib/commands/resolvePerson.ts` (pure, unit-tested) matches a spoken name ("אמא", "עודד") against the user's real `people` rows (exact, then partial, case-insensitive); no match means the route replies honestly that it couldn't find that contact rather than logging an interaction against the wrong person or fabricating one.

**Voice input, a small progressive enhancement, not a new pipeline.** `hooks/useVoiceInput.ts` feature-detects the Web Speech API (`SpeechRecognition`/`webkitSpeechRecognition`, Chrome/Edge only today, no Firefox/Safari support) and only renders a mic button in the Command Panel where it's actually available — nowhere does a broken or silently-failing control exist. A recognized phrase (Hebrew, `he-IL`) is appended to the input text; it is never auto-sent, the same explicit-confirmation discipline as the rest of the panel.

**UI.** The Command Panel lives inside the same `AICompanion` floating `Modal` as regular chat, behind an explicit "שיחה" / "פקודה" (Chat / Command) toggle — a deliberate choice over silently trying to detect "is this message a command" from free text, which would risk misclassifying a normal chat message as a destructive calendar action. The user decides the mode; the panel then decides what to do with it. No new design tokens — same `focus-ring`, accent colors, and bubble styling as the rest of `AICompanion`.

**Verified via** typecheck, lint, the full test suite (225 tests — new coverage on `deriveEnergyLevel`, the DST-aware `timeWindow` math across a winter date, a summer date, and a midnight/month rollover, and `resolvePersonByName`'s exact/partial/no-match cases), a production build, and a live-database smoke test confirming `personal_patterns`' real column types (`confidence` returns as a genuine JS `number`, not a string) match what the energy route assumes. **Not verified:** any of this against a live authenticated browser session, a real `OPENAI_API_KEY` (still unset), a real Google Calendar (the actual `events.list`/`DELETE` calls), or a real microphone — every one of these is a pre-existing, named limitation of this environment, not a new gap introduced here.

**Deliberately not built this milestone:** a general natural-language agent over arbitrary app state — the five supported intents were chosen because each maps to a real, already-safe mutation path, not because they're the only conceivable commands; expanding the set later means adding another bounded intent, not loosening this one's guardrails. Silent/automatic command detection inside regular chat (see the explicit-mode-toggle reasoning above). Persisting command-panel history (ephemeral, session-only local state — same reasoning as chat's "based on" reasoning and Deep Onboarding's skipped-topics list). A `google_event_id` write-back for `upcoming_events` rows cleared via calendar commands — `upcoming_events` and real Google Calendar events are already two different things in this schema (§8), and this milestone didn't need to change that.

---

## 13. Goals Engine timeline & the Relationship CRM

**What it is:** two connected upgrades, delivered together because the milestone's own test scenario connected them — a real dated timeline for goals (not just an unordered checklist), and a real relationship layer for `people` (anniversaries alongside birthdays, goals linkable to a specific person, proactive Today suggestions, and an honestly-scoped meeting coordinator).

**Built on top of in-progress work already in the repo.** A concurrent session had already started generalizing `daysUntilNextBirthday` into `daysUntilNextAnnualDate` (`lib/utils.ts`) — the same "MM-DD annual recurrence" math birthdays and anniversaries both need — before this milestone began. Audited (typecheck + full suite passed against that in-progress state) and continued rather than duplicated or reverted, per explicit instruction once the concurrent edit was surfaced.

**Goals Engine timeline.** `goals.target_date` already existed but was never used for anything; milestones had no date at all. New migration `20260720000009` adds `milestones.due_date`. `lib/goals/distributeMilestoneDates.ts` (new, pure, unit-tested across the wedding-planning test case and a no-target-date/past-target-date honesty check) spreads a goal's milestones evenly between now and its target date — deterministic date arithmetic, not trusted to the model: the AI (`/api/goals/breakdown`, unchanged) still proposes *what* the milestones are, `lib/db/goals.ts`'s `createWithMilestones` now assigns *when* each one is due. No target date → every `due_date` stays `null`, the same "no invented timeline" discipline as every other optional field in this app. `GoalsPanel` gained a target-date input and an optional person picker; `GoalJourneyCard` shows each undone milestone's due date and, when set, the linked person's name.

**Relationship CRM.** `people.anniversary` (same migration, same shape/constraint as `birthday`) and `goals.person_id` (nullable, `on delete set null` — removing a person shouldn't destroy a goal that exists independently of them) are the two new columns. `PersonRelationshipCard` gained a shared `InlineDateField` (one implementation for both the birthday and anniversary "add a date" affordance, not two copies of the same edit/save flow) and now shows an anniversary countdown identically to the existing birthday one.

**Proactive CRM Actions, reusing what already existed rather than adding a new computation.** `lib/context/buildAtlasContext.ts`'s `relationshipSignals` previously only ever said "you haven't contacted X in N days." It now also checks birthday proximity (reusing `pickSuggestedAction`'s own exported `BIRTHDAY_WINDOW_DAYS`, so Today and the Family page agree on what "approaching" means) and produces a real "worth calling" or "worth thinking about a gift" line — both flow into Today's `AIBriefing` through the existing Intelligence Engine pipeline (§9) with zero new wiring, the same way Family Experience v2's staleness signal already did. Deliberately *not* a full `pickSuggestedAction` call here: that needs interaction-trend data (a moments query per person) this broadly-used, called-by-every-AI-route context function was never going to pay for on every request — `isPersonStale` alone is already the identical condition `pickSuggestedAction`'s own `needs_attention` branch checks, so reusing just that predicate is honest, not a shortcut.

**Meeting Coordinator, scoped to what's actually real.** "Mutual availability" isn't something Atlas can check — it only has OAuth access to the signed-in user's own Google Calendar, and there is no mechanism for a second person to connect theirs (that would be a multi-party auth system, a real product decision, not a Today-view feature). `app/api/family/meetup-suggestion` (new) is explicit about this: it finds the user's own real free slots over the next few days (reusing `computeFreeSlots`/`freeBusy`, the same mechanism calendar suggestions already use, plus the DST-aware `jerusalemDateTimeToUtc` from the Command Panel's `lib/commands/timeWindow.ts`) and returns them with a note that says plainly this is *not* a real mutual-availability check. Location suggestions are a real, deterministic time-of-day category (`lib/family/suggestMeetupLocationType.ts` — "café," "restaurant," etc.) — never a fabricated real place, since no Places/Maps API key exists in this app.

**Test scenario, real data.** A real `people` row for יסמין (relation: בת זוג — only the stated facts; no invented birthday/anniversary) and a real `goals` row ("לתכנן חתונה," category `family`, `target_date` 2026-08-09 from the milestone's own "9.8.26" per Israeli DD.MM.YY convention, `person_id` linked to her) were created directly against the live database, with milestones spread by the real `distributeMilestoneDates` algorithm landing exactly on the target date. Milestone *content* is the same honest-fallback generic list `app/api/goals/breakdown` already produces for any real user when `OPENAI_API_KEY` is unset (true in this environment) — presented as such, not dressed up as AI-generated wedding-specific output that couldn't actually be verified here.

**Verified via** typecheck, lint, the full test suite (new coverage on `distributeMilestoneDates` including the wedding-planning date math, and `suggestMeetupLocationType`), a production build, `npm run db:migrate` against live Supabase, a live-database smoke test (throwaway user) covering the anniversary column, the goal→person link, the milestone due date, and the `on delete set null` FK behavior, and the real data-seeding run described above. **Not verified:** the AI's actual breakdown quality for a real macro-goal (no `OPENAI_API_KEY` in this environment — the honest-fallback path is what actually ran), and everything needing a live authenticated browser session (the Family page's new anniversary field, the meeting-coordinator button, a real Google Calendar `freeBusy` call) — the same standing, named gap as every other such item in this document.

**Deliberately not built this milestone:** a `google_event_id`-style real link between `people`/anniversaries and Google Calendar (no feature needs it yet); extending the AI Command Panel's `add_goal` intent to also accept a target date or person link (a real, natural follow-up, out of this milestone's explicit scope); any real Places/Maps integration for the meeting coordinator (needs an API key this environment doesn't have — asked for, not assumed); a genuine multi-party "does the other person also have this time free" check (needs the other person to connect their own calendar to Atlas, a real multi-tenant/multi-party auth decision, not a Today-view feature — named here as the actual gate, not silently worked around).

---

## 14. What this document is not

It is not a rewrite plan. Nothing in `app/`, `components/`, `lib/`, or `store/` needs to change shape to support §1–§13 — every "target" above is additive to what exists, and most of it (§2 v1, the personalDNA wiring in §3, the agent-shaped routes in §4, the Intelligence Engine in §9, the Experience Layer in §10, Deep Onboarding in §11, Today Dashboard v2/the Command Panel in §12, the Goals timeline/Relationship CRM in §13) either already works or is a small, bounded next step. Re-read `docs/BACKLOG.md` and `docs/ROADMAP_V2.md` for what's actually being worked on next; treat this document as the map those decisions get checked against, not a new backlog to work through top-to-bottom.
